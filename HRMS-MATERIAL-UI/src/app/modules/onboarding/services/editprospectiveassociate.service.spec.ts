import { TestBed } from '@angular/core/testing';

import { EditprospectiveassociateService } from './editprospectiveassociate.service';

describe('EditprospectiveassociateService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: EditprospectiveassociateService = TestBed.get(EditprospectiveassociateService);
    expect(service).toBeTruthy();
  });
});
