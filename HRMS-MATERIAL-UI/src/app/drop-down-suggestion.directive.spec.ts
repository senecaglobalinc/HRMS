import { DropDownSuggestionDirective } from './drop-down-suggestion.directive';

describe('DropDownSuggestionDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownSuggestionDirective();
    expect(directive).toBeTruthy();
  });
});
