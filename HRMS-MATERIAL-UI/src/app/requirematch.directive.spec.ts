import { RequirematchDirective } from './requirematch.directive';

describe('RequirematchDirective', () => {
  it('should create an instance', () => {
    const directive = new RequirematchDirective();
    expect(directive).toBeTruthy();
  });
});
